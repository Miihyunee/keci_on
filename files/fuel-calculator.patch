diff --git a/README.md b/README.md
index 690f95a..5b90cf4 100644
--- a/README.md
+++ b/README.md
@@ -61,7 +61,7 @@ script.js    — 여비 계산 로직, UI 제어, PDF 출력
 | 숙박비 (법인) | `lodgingCorpNights` / `lodgingCorpAmount` | 법인카드 결제분 |
 | 식사 제공 공제 | `freeMeals` | 제공 횟수당 8,300원 공제 (근무지 외일 때만 적용) |
 | 기본 여객 운임비 | `transportFarePersonal` / `transportFareCorp` | 대중교통 개인·법인 |
-| 업무 주유 정산비 | `transportFuelPersonal` / `transportFuelCorp` | 대중교통 선택 시 비활성 |
+| 업무 주유 정산비 | `transportFuelPersonal` / `transportFuelCorp` | 대중교통 선택 시 비활성. 라벨 옆 **유류비 계산기** 버튼으로 오피넷 연동 자동 계산 패널 오픈 (아래 "유류비 자동 계산기" 절 참고) |
 | 시설 유료 주차비 | `transportParkingPersonal` / `transportParkingCorp` | 대중교통 선택 시 비활성 |
 | 하이패스 통행요금 | `transportHipassPersonal` / `transportHipassCorp` | 대중교통 선택 시 비활성 |
 
@@ -173,6 +173,53 @@ script.js    — 여비 계산 로직, UI 제어, PDF 출력
 
 ---
 
+## 유류비 자동 계산기 (한국석유공사 오피넷 Open API 연동)
+
+"업무 주유 정산비" 라벨 우측의 **유류비 계산기** 버튼(`toggleFuelCalc()`)을 누르면 계산 패널이 열립니다. 대중교통 이용 시에는 버튼이 비활성화되고 패널이 자동으로 닫힙니다(`onVehicleChange()`에서 처리).
+
+### 계산 방식
+
+```
+예상 유류비 = (이동 거리 ÷ 차량 연비) × 리터당 단가
+```
+
+- **리터당 단가**는 오피넷 API로 자동 조회하거나 직접 입력할 수 있습니다.
+- 계산 결과는 "개인 정산비에 반영" / "법인 정산비에 반영" 버튼으로 `transportFuelPersonal` / `transportFuelCorp` 입력값에 **더해서** 반영됩니다(기존 입력값 보존).
+
+### 오피넷 API 연동
+
+| 항목 | 내용 |
+|---|---|
+| 사용 API | 오피넷 전국 주유소 평균가격 (`avgAllPrice.do`) |
+| 요청 URL | `https://www.opinet.co.kr/api/avgAllPrice.do?out=json&code={인증키}` |
+| 인증키 발급 | [오피넷 API 신청 페이지](https://www.opinet.co.kr/user/custapi/custApiInfo.do) (회원가입 후 무료 발급) |
+| 지원 유종 | 휘발유(B027) · 경유(D047) · 고급휘발유(B034) · LPG(K015) |
+| 인증키 저장 위치 | 브라우저 `localStorage`(`opinetApiKey`)에만 저장. 서버로 전송되지 않음 |
+
+`fetchOpinetPrice()`가 위 URL을 호출해 응답(JSON)의 `RESULT.OIL` 목록에서 선택한 유종의 `PRICE`(리터당 평균가)를 찾아 "리터당 단가" 입력란에 채워 넣고, `updateFuelCalcResult()`가 계산 결과를 갱신합니다.
+
+### 인증키를 매번 입력하지 않으려면
+
+`getOpinetApiKey()`는 다음 우선순위로 인증키를 결정합니다.
+
+1. 인증키 입력창에 지금 타이핑되어 있는 값
+2. 브라우저 `localStorage`에 저장된 값 (한 번 "저장" 버튼을 누르면 이후 자동 유지)
+3. `script.js` 상단의 `DEFAULT_OPINET_API_KEY` 상수에 고정해 둔 값
+
+즉, 매번 입력·저장하는 대신 `script.js`의 아래 상수에 발급받은 키를 직접 넣어두면 페이지를 열 때마다 자동으로 채워집니다.
+
+```js
+const DEFAULT_OPINET_API_KEY = '여기에_발급받은_인증키';
+```
+
+> ⚠️ 이 프로젝트는 백엔드가 없는 정적 페이지이므로, `DEFAULT_OPINET_API_KEY`에 넣은 값은 브라우저 "페이지 소스 보기"로 누구나 확인할 수 있습니다. 저장소가 공개(public)라면 다른 사람이 키를 복사해 갈 수 있다는 점을 감안해 주세요.
+
+> ⚠️ **CORS 관련 주의사항**: 오피넷 서버가 브라우저 간 교차 출처 요청(CORS)을 허용하지 않는 경우 조회가 실패할 수 있으며, 이 경우 안내 메시지가 표시됩니다. 이때도 "리터당 단가"를 직접 입력하면 계산기 자체는 동일하게 동작합니다. 안정적인 자동 조회가 꼭 필요하다면 오피넷 API를 대신 호출해주는 별도의 서버(프록시)를 두는 방법이 있습니다.
+
+> ℹ️ 오피넷이 제공하는 값은 **전국 평균가**이며, 실제 주유소 판매 가격과 다를 수 있는 참고용 수치입니다.
+
+---
+
 ## 이벤트 흐름
 
 ```
diff --git a/index.html b/index.html
index ca77505..11e2b5a 100644
--- a/index.html
+++ b/index.html
@@ -286,7 +286,12 @@
               </div>
 
               <div class="form-field" id="tFieldFuel">
-                <label>업무 주유 정산비</label>
+                <label style="display:flex; align-items:center; justify-content:space-between; gap:6px;">
+                  <span>업무 주유 정산비</span>
+                  <button type="button" id="fuelCalcToggleBtn" onclick="toggleFuelCalc()" title="오피넷 실시간 유가로 예상 유류비를 계산합니다" style="background:#f0f9ff; border:1px solid #7dd3fc; color:#0284c7; border-radius:6px; font-size:11px; font-weight:600; padding:3px 8px; cursor:pointer; white-space:nowrap; display:inline-flex; align-items:center; gap:4px;">
+                    <i class="fa-solid fa-calculator"></i> 유류비 계산기
+                  </button>
+                </label>
                 <div class="split-inputs">
                   <div class="split-row">
                     <span class="split-label personal-label">개인</span>
@@ -344,6 +349,84 @@
                   </div>
                 </div>
               </div>
+
+              <!-- 유류비 자동 계산기 (한국석유공사 오피넷 Open API 연동) -->
+              <div class="form-field full-width" id="fuelCalcPanel" style="display:none; margin-top:4px; background:#f8fafc; border:1px solid #e2e8f0; border-radius:8px; padding:16px 18px;">
+                <div style="display:flex; align-items:center; justify-content:space-between; margin-bottom:10px;">
+                  <div style="font-weight:700; font-size:14px; color:#0f172a;">
+                    <i class="fa-solid fa-gas-pump" style="color:#0284c7;"></i>
+                    유류비 자동 계산기 <span style="font-weight:400; color:#64748b; font-size:12px;">(한국석유공사 오피넷 Open API 연동)</span>
+                  </div>
+                  <button type="button" onclick="toggleFuelCalc()" style="background:none; border:none; color:#94a3b8; font-size:16px; cursor:pointer; line-height:1;">&times;</button>
+                </div>
+
+                <!-- 인증키 -->
+                <div style="display:flex; align-items:flex-end; gap:8px; flex-wrap:wrap; margin-bottom:12px;">
+                  <div style="flex:1; min-width:220px;">
+                    <label style="font-size:12px; color:#475569; font-weight:600; display:block; margin-bottom:4px;">오피넷 API 인증키</label>
+                    <input type="text" id="opinetApiKey" placeholder="오피넷에서 발급받은 인증키를 입력하세요 (script.js의 DEFAULT_OPINET_API_KEY에 고정도 가능)" style="width:100%; height:36px; border:1px solid #cbd5e1; border-radius:6px; padding:0 10px; font-size:13px; box-sizing:border-box;">
+                  </div>
+                  <button type="button" class="btn" onclick="saveOpinetKey()" style="background:#e2e8f0; color:#334155; height:36px; padding:0 14px; border:none; border-radius:6px; font-size:13px; cursor:pointer;">저장</button>
+                  <a href="https://www.opinet.co.kr/user/custapi/custApiInfo.do" target="_blank" rel="noopener" style="font-size:12px; color:#0284c7; height:36px; display:flex; align-items:center; white-space:nowrap;">인증키 발급받기 <i class="fa-solid fa-arrow-up-right-from-square" style="margin-left:4px; font-size:10px;"></i></a>
+                </div>
+
+                <!-- 유종 선택 + 조회 버튼 -->
+                <div style="display:flex; align-items:flex-end; gap:8px; flex-wrap:wrap; margin-bottom:12px;">
+                  <div style="min-width:160px;">
+                    <label style="font-size:12px; color:#475569; font-weight:600; display:block; margin-bottom:4px;">유종</label>
+                    <select id="fuelProductCode" onchange="updateFuelCalcResult()" style="width:100%; height:36px; border:1px solid #cbd5e1; border-radius:6px; padding:0 8px; font-size:13px;">
+                      <option value="B027" selected>휘발유</option>
+                      <option value="D047">경유</option>
+                      <option value="B034">고급휘발유</option>
+                      <option value="K015">LPG(자동차용부탄)</option>
+                    </select>
+                  </div>
+                  <button type="button" class="btn" onclick="fetchOpinetPrice()" style="background:#0284c7; color:#fff; height:36px; padding:0 14px; border:none; border-radius:6px; font-size:13px; cursor:pointer;">
+                    <i class="fa-solid fa-cloud-arrow-down"></i>&nbsp;전국 평균 유가 실시간 조회
+                  </button>
+                  <span id="fuelApiStatus" style="font-size:12px; color:#64748b;"></span>
+                </div>
+
+                <!-- 계산 입력값 -->
+                <div style="display:grid; grid-template-columns:repeat(3, 1fr); gap:12px; margin-bottom:10px;">
+                  <div>
+                    <label style="font-size:12px; color:#475569; font-weight:600; display:block; margin-bottom:4px;">리터당 단가 (자동조회 또는 직접입력)</label>
+                    <div class="input-wrap">
+                      <input type="text" inputmode="numeric" pattern="[0-9]*" id="fuelUnitPrice" oninput="this.value=this.value.replace(/[^0-9]/g,''); updateFuelCalcResult()" style="width:100%; height:36px; border:1px solid #cbd5e1; border-radius:6px; padding:0 32px 0 10px; font-size:13px; box-sizing:border-box;">
+                      <span class="unit">원/L</span>
+                    </div>
+                  </div>
+                  <div>
+                    <label style="font-size:12px; color:#475569; font-weight:600; display:block; margin-bottom:4px;">이동 거리</label>
+                    <div class="input-wrap">
+                      <input type="text" inputmode="numeric" pattern="[0-9]*" id="fuelDistance" oninput="this.value=this.value.replace(/[^0-9]/g,''); updateFuelCalcResult()" style="width:100%; height:36px; border:1px solid #cbd5e1; border-radius:6px; padding:0 24px 0 10px; font-size:13px; box-sizing:border-box;">
+                      <span class="unit">km</span>
+                    </div>
+                  </div>
+                  <div>
+                    <label style="font-size:12px; color:#475569; font-weight:600; display:block; margin-bottom:4px;">차량 연비</label>
+                    <div class="input-wrap">
+                      <input type="text" inputmode="decimal" id="fuelEfficiency" value="10" oninput="this.value=this.value.replace(/[^0-9.]/g,''); updateFuelCalcResult()" style="width:100%; height:36px; border:1px solid #cbd5e1; border-radius:6px; padding:0 36px 0 10px; font-size:13px; box-sizing:border-box;">
+                      <span class="unit">km/L</span>
+                    </div>
+                  </div>
+                </div>
+
+                <!-- 결과 및 반영 -->
+                <div style="display:flex; align-items:center; justify-content:space-between; flex-wrap:wrap; gap:10px; background:#fff; border:1px solid #e2e8f0; border-radius:6px; padding:10px 14px;">
+                  <div style="font-size:13px; color:#334155;">
+                    예상 유류비 = 거리 ÷ 연비 × 리터당 단가 = <strong id="fuelCalcResult" style="color:#0284c7; font-size:16px;">0원</strong>
+                  </div>
+                  <div style="display:flex; gap:8px;">
+                    <button type="button" onclick="applyFuelCost('Personal')" style="background:#fff; border:1px solid #0284c7; color:#0284c7; border-radius:6px; font-size:12px; font-weight:600; padding:7px 12px; cursor:pointer;">개인 정산비에 반영</button>
+                    <button type="button" onclick="applyFuelCost('Corp')" style="background:#fff; border:1px solid #0284c7; color:#0284c7; border-radius:6px; font-size:12px; font-weight:600; padding:7px 12px; cursor:pointer;">법인 정산비에 반영</button>
+                  </div>
+                </div>
+
+                <div style="font-size:11px; color:#94a3b8; margin-top:8px; line-height:1.5;">
+                  ※ 인증키는 이 브라우저에만 저장되며 서버로 전송되지 않습니다. 오피넷 API는 전국 평균 판매가격을 제공하며(실제 주유소 가격과 차이가 있을 수 있음), 브라우저 보안 정책(CORS)에 따라 조회가 실패할 수 있습니다 — 이 경우 리터당 단가를 직접 입력해 계산할 수 있습니다.
+                </div>
+              </div>
             </div>
           </div>
 
diff --git a/script.js b/script.js
index f5ebfe0..40fdc26 100644
--- a/script.js
+++ b/script.js
@@ -307,6 +307,9 @@ function onVehicleChange() {
         'transportHipassPersonal', 'transportHipassCorp'
     ];
 
+    const fuelCalcToggleBtn = document.getElementById('fuelCalcToggleBtn');
+    const fuelCalcPanel = document.getElementById('fuelCalcPanel');
+
     if (vehicleEl.value === 'public') { 
         carFields.forEach(id => {
             const el = document.getElementById(id);
@@ -315,11 +318,219 @@ function onVehicleChange() {
                 el.disabled = true;  
             }
         });
+        // 대중교통 이용 시 유류비 계산기는 의미가 없으므로 비활성화하고 패널을 닫습니다.
+        if (fuelCalcToggleBtn) {
+            fuelCalcToggleBtn.disabled = true;
+            fuelCalcToggleBtn.style.opacity = '0.5';
+            fuelCalcToggleBtn.style.cursor = 'not-allowed';
+        }
+        if (fuelCalcPanel) fuelCalcPanel.style.display = 'none';
     } else { 
         carFields.forEach(id => {
             const el = document.getElementById(id);
             if (el) el.disabled = false; 
         });
+        if (fuelCalcToggleBtn) {
+            fuelCalcToggleBtn.disabled = false;
+            fuelCalcToggleBtn.style.opacity = '1';
+            fuelCalcToggleBtn.style.cursor = 'pointer';
+        }
+    }
+
+    updatePreview();
+}
+
+/* ============================================================
+ * 유류비 자동 계산기 (한국석유공사 오피넷 Open API 연동)
+ * ------------------------------------------------------------
+ * - "전국 주유소 평균가격" API(avgAllPrice.do)를 호출하여 유종별
+ *   리터당 평균 단가를 조회합니다.
+ * - 예상 유류비 = 이동 거리(km) ÷ 차량 연비(km/L) × 리터당 단가
+ * - 오피넷 API는 브라우저에서 직접 호출할 경우 CORS 정책에 의해
+ *   차단될 수 있습니다. 이 경우 리터당 단가를 직접 입력하면
+ *   계산기는 동일하게 동작합니다.
+ * ============================================================ */
+const OPINET_PRODUCTS = {
+    B027: '휘발유',
+    D047: '경유',
+    B034: '고급휘발유',
+    K015: 'LPG(자동차용부탄)'
+};
+
+/**
+ * 오피넷 API 인증키를 코드에 직접 고정해두고 싶다면 아래 따옴표 안에 값을 넣어주세요.
+ * 예: const DEFAULT_OPINET_API_KEY = 'a1b2c3d4e5f6...';
+ * ⚠️ 정적 페이지 특성상 이 값은 브라우저에서 페이지 소스를 통해 누구나 볼 수 있습니다.
+ *    저장소가 공개(public) 상태라면 유출될 수 있다는 점을 감안해 주세요.
+ */
+const DEFAULT_OPINET_API_KEY = '';
+
+/**
+ * 실제 사용할 인증키를 결정합니다.
+ * 우선순위: 입력창에 지금 타이핑된 값 > 브라우저에 저장된 값(localStorage) > 코드에 고정된 기본값
+ */
+function getOpinetApiKey() {
+    const keyInput = document.getElementById('opinetApiKey');
+    const typed = keyInput ? keyInput.value.trim() : '';
+    if (typed) return typed;
+
+    const saved = localStorage.getItem('opinetApiKey');
+    if (saved) return saved;
+
+    return DEFAULT_OPINET_API_KEY.trim();
+}
+
+/**
+ * 유류비 계산기 패널을 열고 닫습니다.
+ */
+function toggleFuelCalc() {
+    const btn = document.getElementById('fuelCalcToggleBtn');
+    if (btn && btn.disabled) return; // 대중교통 선택 시에는 열리지 않음
+
+    const panel = document.getElementById('fuelCalcPanel');
+    if (!panel) return;
+
+    const opening = panel.style.display === 'none' || !panel.style.display;
+    panel.style.display = opening ? 'block' : 'none';
+
+    if (opening) {
+        // 인증키 입력창이 비어 있다면, 저장된 값 또는 코드에 고정된 기본값을 자동으로 채웁니다.
+        const keyInput = document.getElementById('opinetApiKey');
+        if (keyInput && !keyInput.value) {
+            const key = getOpinetApiKey();
+            if (key) keyInput.value = key;
+        }
+
+        // 서식 초기화 등으로 연비 값이 비어있으면 기본값(10km/L)을 채워줍니다.
+        const effInput = document.getElementById('fuelEfficiency');
+        if (effInput && !effInput.value) effInput.value = '10';
+
+        updateFuelCalcResult();
+    }
+}
+
+/**
+ * 오피넷 인증키를 이 브라우저(localStorage)에 저장합니다. 서버로는 전송되지 않습니다.
+ */
+function saveOpinetKey() {
+    const keyInput = document.getElementById('opinetApiKey');
+    const statusEl = document.getElementById('fuelApiStatus');
+    if (!keyInput) return;
+
+    const key = keyInput.value.trim();
+    if (!key) {
+        alert('저장할 인증키를 입력해 주세요.');
+        return;
+    }
+    localStorage.setItem('opinetApiKey', key);
+    if (statusEl) {
+        statusEl.style.color = '#16a34a';
+        statusEl.innerText = '인증키가 이 브라우저에 저장되었습니다.';
+    }
+}
+
+/**
+ * 오피넷 "전국 주유소 평균가격" API를 호출하여 선택한 유종의 리터당 단가를 자동으로 채워줍니다.
+ */
+async function fetchOpinetPrice() {
+    const statusEl = document.getElementById('fuelApiStatus');
+    const keyInput = document.getElementById('opinetApiKey');
+    const productSelect = document.getElementById('fuelProductCode');
+    const key = getOpinetApiKey();
+    const productCode = productSelect ? productSelect.value : 'B027';
+
+    if (!key) {
+        alert('오피넷 API 인증키가 없습니다. 입력창에 인증키를 넣거나, script.js 상단의 DEFAULT_OPINET_API_KEY에 고정해 주세요. ("인증키 발급받기" 링크에서 무료로 발급받을 수 있습니다.)');
+        return;
+    }
+    // 입력창에 값이 비어 있다면(기본값을 사용 중이라면) 화면에도 채워서 보여줍니다.
+    if (keyInput && !keyInput.value) keyInput.value = key;
+    localStorage.setItem('opinetApiKey', key);
+
+    if (statusEl) {
+        statusEl.style.color = '#64748b';
+        statusEl.innerText = '실시간 유가를 조회하는 중입니다...';
+    }
+
+    const url = `https://www.opinet.co.kr/api/avgAllPrice.do?out=json&code=${encodeURIComponent(key)}`;
+
+    try {
+        const controller = new AbortController();
+        const timeoutId = setTimeout(() => controller.abort(), 8000);
+        const res = await fetch(url, { signal: controller.signal });
+        clearTimeout(timeoutId);
+
+        if (!res.ok) throw new Error(`HTTP ${res.status}`);
+        const data = await res.json();
+
+        const list = data && data.RESULT && data.RESULT.OIL ? data.RESULT.OIL : null;
+        if (!list) throw new Error('응답 형식 오류');
+
+        const items = Array.isArray(list) ? list : [list];
+        const matched = items.find(item => item.PRODCD === productCode);
+        if (!matched) throw new Error('해당 유종 데이터를 찾을 수 없습니다');
+
+        const price = Math.round(parseFloat(matched.PRICE));
+        const priceInput = document.getElementById('fuelUnitPrice');
+        if (priceInput) priceInput.value = price;
+
+        if (statusEl) {
+            statusEl.style.color = '#16a34a';
+            statusEl.innerText = `조회 완료 (${OPINET_PRODUCTS[productCode] || productCode} · 기준일 ${matched.TRADE_DT || '-'}) — ${price.toLocaleString()}원/L`;
+        }
+
+        updateFuelCalcResult();
+    } catch (err) {
+        // 오피넷 API는 브라우저 직접 호출 시 CORS 정책에 의해 차단될 수 있습니다.
+        if (statusEl) {
+            statusEl.style.color = '#dc2626';
+            statusEl.innerText = '실시간 조회에 실패했습니다 (인증키 오류 또는 브라우저 보안 정책(CORS) 차단일 수 있습니다). 아래 "리터당 단가"에 직접 입력해 계산을 진행해 주세요.';
+        }
+    }
+}
+
+/**
+ * 이동 거리 ÷ 차량 연비 × 리터당 단가로 예상 유류비를 계산하여 화면에 표시합니다.
+ */
+function updateFuelCalcResult() {
+    const resultEl = document.getElementById('fuelCalcResult');
+    if (!resultEl) return 0;
+
+    const unitPrice = parseInt((document.getElementById('fuelUnitPrice') || {}).value || '0', 10) || 0;
+    const distance = parseInt((document.getElementById('fuelDistance') || {}).value || '0', 10) || 0;
+    const efficiency = parseFloat((document.getElementById('fuelEfficiency') || {}).value || '0') || 0;
+
+    const cost = efficiency > 0 ? Math.round((distance / efficiency) * unitPrice) : 0;
+    resultEl.innerText = `${cost.toLocaleString()}원`;
+    return cost;
+}
+
+/**
+ * 계산된 예상 유류비를 업무 주유 정산비(개인/법인) 입력값에 더해 반영합니다.
+ */
+function applyFuelCost(type) {
+    const vehicleEl = document.getElementById('vehicle');
+    if (vehicleEl && vehicleEl.value === 'public') {
+        alert('대중교통 이용 시에는 주유 정산비를 반영할 수 없습니다. 지원 이동차량 구분을 확인해 주세요.');
+        return;
+    }
+
+    const cost = updateFuelCalcResult();
+    if (!cost || cost <= 0) {
+        alert('반영할 유류비가 0원입니다. 리터당 단가, 이동 거리, 연비를 확인해 주세요.');
+        return;
+    }
+
+    const targetEl = document.getElementById(`transportFuel${type}`);
+    if (!targetEl) return;
+
+    const current = parseInt(targetEl.value.replace(/[^0-9]/g, '') || '0', 10) || 0;
+    targetEl.value = current + cost;
+
+    const statusEl = document.getElementById('fuelApiStatus');
+    if (statusEl) {
+        statusEl.style.color = '#16a34a';
+        statusEl.innerText = `${type === 'Personal' ? '개인' : '법인'} 정산비에 ${cost.toLocaleString()}원이 반영되었습니다.`;
     }
 
     updatePreview();
